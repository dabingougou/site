---
import '../styles/global.css';
const base = import.meta.env.BASE_URL;
const basePrefix = base.endsWith('/') ? base : base + '/';
const href = (path = '') => basePrefix + String(path).replace(/^\/+/, '');
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Huibin Chang</title>
  </head>
  <body>
    <main class="container">
      <h1>Huibin's Personal Site</h1>
      <p>
        This is where I host stuff I build, write, and keep around for future me.
      </p>

      <nav>
        <a href={href('about/')}>About</a> ·
        <a href={href('projects/')}>Projects</a> ·
        <a href={href('papers/')}>Papers</a> ·
        <a href={href('writing/')}>Writing</a> ·
        <a href={href('media/')}>Media</a>
      </nav>

      <h2>About Me</h2>
      <p>
        I trained as a research economist and now work as a government economist and data scientist. I am a husband, father, and son. I live in Victoria, BC, Canada. I also love music with a good guitar sound in it, and I like tinkering with Raspberry Pi, including hosting a WireGuard setup for friends who need secure access.
      </p>
    </main>
  </body>
</html>
